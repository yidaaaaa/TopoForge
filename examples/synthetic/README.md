# Synthetic fixtures

`gaussian-hill.tif` is a deterministic analytic DTM-like test surface in EPSG:32648 with a synthetic local vertical zero. It is not a real location and is Apache-2.0 licensed. Reproduce the validated bundle with the README commands and compare against `artifacts/reports/milestone-01-{validation,provenance}.json`.
